# TODO - Будущие версии

## v1.5.0 - Выбор режима группировки по папкам

**Проблема:** В GUI сейчас только checkbox для включения группировки, режим жёстко задан как `'year-month'`.

**Текущий код:** `rename_media_gui.py:885`
```python
folder_organizer = FolderOrganizer('year-month') if self.organize_folders.get() else None
```

**Требуемые изменения:**

1. **GUI (`rename_media_gui.py`)**
   - Добавить выпадающий список (ttk.Combobox) с режимами:
     - `Без группировки` → `'none'`
     - `Только по годам` → `'year'`
     - `По годам и месяцам` (по умолчанию) → `'year-month'`
     - `Полная дата` → `'date'`
   - Убрать checkbox, заменить на Combobox
   - Передавать выбранный режим в `FolderOrganizer(mode)`

2. **Тестирование**
   - Проверить все 4 режима
   - Проверить, что `'none'` отключает создание папок

**Приоритет:** Высокий (запрос пользователей)

---

## Другие идеи для будущих версений

- [ ] Параллельная обработка файлов (многопоточность/многопроцессность)
- [ ] Экспорт отчёта в CSV/HTML
- [ ] Thumbnail preview в GUI
- [ ] Drag-and-drop в GUI
- [ ] Больше вариантов извлечения метаданных
